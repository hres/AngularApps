import { AriaTransformPipe } from './aria-transform.pipe';

describe('AriaTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new AriaTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
