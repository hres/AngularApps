import { FormGroupPipe } from './form-group.pipe';

describe('FormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
